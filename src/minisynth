#!/usr/bin/env python3
import sys, getopt

def main(argv):
	#//print 'Number of arguments:', len(sys.argv), 'arguments.'
	#print 'Argument List:', str(sys.argv)

	f = open("examples/Simple_Tune.synth", "r")
	print(f.read())

if __name__ == "__main__":
	main(sys.argv[1:])
